/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        // Single accent for the monochrome theme: a desaturated zinc highlight.
        accent: {
          DEFAULT: "#e4e4e7", // zinc-200, used sparingly for active states
          subtle: "#3f3f46", // zinc-700, for hover surfaces
        },
      },
      fontFamily: {
        sans: ["ui-sans-serif", "system-ui", "-apple-system", "Segoe UI", "Roboto", "sans-serif"],
        mono: ["ui-monospace", "SFMono-Regular", "Menlo", "monospace"],
      },
    },
  },
  plugins: [],
};
